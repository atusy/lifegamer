% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/first_generation.R
\name{first_generation}
\alias{first_generation}
\alias{first_generation.character}
\alias{first_generation.random}
\alias{first_generation.grider}
\alias{first_generation.segment10}
\alias{first_generation.octagon}
\title{Generate First Generation for Cellular Automaton}
\usage{
first_generation(kind = first_generation_kinds, ...)

\method{first_generation}{character}(kind = "random", ...)

\method{first_generation}{random}(kind = "random", nrow = 50, ncol = 50, p = 0.3, ...)

\method{first_generation}{grider}(kind = "grider", nrow = 50, ncol = 50, ...)

\method{first_generation}{segment10}(kind = "segment10", ...)

\method{first_generation}{octagon}(kind = "octagon", ...)
}
\arguments{
\item{kind}{A character string specifying the type of initial pattern.
One of \code{first_generation_kinds}: "random", "grider", "segment10", or "octagon".}

\item{...}{Additional arguments passed to methods.}

\item{nrow}{Number of rows in the grid (default: 50).}

\item{ncol}{Number of columns in the grid (default: 50).}

\item{p}{Probability of a cell being alive in random generation (0 to 1).}
}
\value{
A numeric matrix representing the initial grid state, where 1
indicates a live cell and 0 indicates a dead cell.
}
\description{
Creates the initial state (first generation) of a cellular automaton grid
using various predefined patterns.
}
\details{
The "grider" pattern creates a classic glider in the top-left corner,
which moves diagonally across the grid over successive generations.

The "segment10" pattern creates a horizontal line of 10 live cells
centered in an 11x18 grid. This pattern oscillates (blinker behavior).

The "octagon" pattern creates an 8x8 octagonal shape, which is a
stable "still life" pattern in Conway's Game of Life.
}
\examples{
# Generate a random first generation
first_generation("random", nrow = 10, ncol = 10, p = 0.3)

# Generate a glider pattern
first_generation("grider")

# Generate an octagon pattern
first_generation("octagon")

}
